spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/monew_postgres
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop  # 또는 update (권장하지 않음)
    show-sql: true

  data:
    mongodb:
      uri: mongodb://localhost:27017/monew_mongo
logging:
  level:
    root: info